<Window x:Class="OnlineStoreApp.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <DataGrid x:Name="ProductsDataGrid" AutoGenerateColumns="False" CanUserAddRows="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding name}" Width="*" />
                <DataGridTextColumn Header="Price" Binding="{Binding price}" Width="*" />
                <DataGridTextColumn Header="Size" Binding="{Binding size}" Width="*" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding quantity}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Save Changes" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="SaveChanges_Click"/>
    </Grid>
</Window>
